class Calculator {
  constructor() {}
  addTwoNumbers(first: number, second: number): number {
    return first + second;
  }
  productTwoNumber(first: number, second: number): number {
    return first * second;
  }
  
}
